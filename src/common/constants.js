export const FULL_ACCESS_CLOUD_PERMISSION_COOKIE = 'full-access-cloud-permission'
export const CANVAS = '$canvas'
export const DEFAULT_CODE = `/* Do not delete this lines. This is the initialization of the canvas. */
var canvas = $canvas;
var context = canvas.getContext('2d');

// Write your code here...
`
